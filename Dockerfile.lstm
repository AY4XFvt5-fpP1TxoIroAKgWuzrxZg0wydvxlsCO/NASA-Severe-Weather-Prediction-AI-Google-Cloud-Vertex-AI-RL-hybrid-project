FROM gcr.io/deeplearning-platform-release/tf2-gpu.2-12:latest
WORKDIR /app
COPY trainer_requirements.txt .
RUN pip install --upgrade pip && pip install -r trainer_requirements.txt
COPY . .
ENTRYPOINT ["python", "train_lstm.py"]
